[install]
linker = "hoisted"

[test]
# lcov feeds scripts/check-coverage.ts, which is what actually gates CI: bunfig's
# coverageThreshold does not affect bun test's exit code (checked on bun 1.3.11).
coverageReporter = ["text", "lcov"]
coverageDir = "coverage"
coverageSkipTestFiles = true
